version: "3.5"

services:
  dnsmasq:
    image: andyshinn/dnsmasq
    restart: unless-stopped
    ports:
      #- 53:53/tcp
      - 53:53/udp
    volumes:
      - ./config/:/etc/dnsmasq.d/:ro  # include every *.conf here by default dnsmasq setup.
    networks:
      - front-end

