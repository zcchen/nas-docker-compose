version: "3.5"

networks:

  front-end:
    ipam:
      driver: default
      config:
        - subnet: 172.16.2.0/24

  mid-layout:
    internal: true
    ipam:
      driver: default
      config:
        - subnet: 172.16.12.0/24

  back-end:
    internal: true
    ipam:
      driver: default
      config:
        - subnet: 172.16.22.0/24

